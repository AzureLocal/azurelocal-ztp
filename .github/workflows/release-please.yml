name: Release Please

# PLACEHOLDER — see repo-management/automation.md for the intended implementation.
# Slim caller for the org-wide reusable workflow:
#   uses: AzureLocal/.github/.github/workflows/reusable-release-please.yml@main
# Currently a no-op until the repo is ready to start cutting releases.

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "release-please.yml is a placeholder. See repo-management/automation.md."
